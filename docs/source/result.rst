causalicp.Result
================

The result of running :meth:`causalicp.fit` is returned in a `causalicp.Result` object, which contains the estimate, accepted sets, p-values, etc.

.. autoclass:: causalicp.Result
   :members:
